import actionTypes from "./actionTypes";

/**
 * Define all of your actions here.
 *
 * - Example:
 *
 *   export function name(params) {
 *     return dispatch => {
 *       dispatch({});
 *     });
 *   }
 */
