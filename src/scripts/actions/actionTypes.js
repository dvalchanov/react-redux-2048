import {generateActions} from "../utils/actions";

/**
 * A place for all your action types.
 *
 * Use `../utils/actions` to add more pre/post action types.
 *
 * - Example:
 *   ["LOGIN"]  becomes:  {LOGIN => "LOGIN"}
 *
 * - Use it as - `actionTypes.LOGIN`
 */
const actions = [];

export default generateActions(actions);
